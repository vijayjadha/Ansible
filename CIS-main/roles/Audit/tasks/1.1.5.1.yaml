# 1.1.5.1 Ensure separate partition exists for /var/log

- name: set var
  set_fact:
    task_name: "Ensure separate partition exists for /var/log"
    task_id: "1.1.5.1"

- name: 1.1.5.1 Ensure separate partition exists for /var/log
  shell: findmnt --kernel /var/log
  register: grep_result
  ignore_errors: true

- name: Display
  set_fact:
    tasks_status: "Passed"
  when: grep_result.stdout.find('/var/log') != -1

- name: Add result to task_results
  set_fact:
    task_results: "{{ task_results | default([]) + [{'task_id': task_id, 'task_name': task_name, 'tasks_status': tasks_status}] }}"

- name: unset var
  set_fact:
    tasks_status: "Failed"
    task_name: ""
    task_id: ""
