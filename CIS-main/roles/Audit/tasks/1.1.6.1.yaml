# 1.1.6.1 Ensure separate partition exists for /var/log/audit

- name: set var
  set_fact:
    task_name: "Ensure separate partition exists for /var/log/audit"
    task_id: "1.1.6.1"

- name: 1.1.6.1 Ensure separate partition exists for /var/log/audit
  shell: findmnt --kernel /var/log/audit
  register: grep_result
  ignore_errors: true

- name: Display
  set_fact:
    tasks_status: "Passed"
  when: grep_result.stdout.find('/var/log/audit') != -1

- name: Add result to task_results
  set_fact:
    task_results: "{{ task_results | default([]) + [{'task_id': task_id, 'task_name': task_name, 'tasks_status': tasks_status}] }}"

- name: unset var
  set_fact:
    tasks_status: "Failed"
    task_name: ""
    task_id: ""
